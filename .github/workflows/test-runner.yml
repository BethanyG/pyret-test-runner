name: Run Tests in Docker

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run test-runner
        run: docker-compose run pyret-test-runner